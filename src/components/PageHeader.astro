---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<header class="mb-10">
  <h1 class="font-ui text-3xl font-bold tracking-tight text-accent-500">{title}</h1>
  {subtitle && <p class="mt-2 text-lg text-base-300">{subtitle}</p>}
</header>
